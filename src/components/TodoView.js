import React from 'react'
import PropTypes from 'prop-types'

const TodoView = props => {
    return (
        <div>
            
        </div>
    )
}

// TodoView.propTypes = {

// }

export default TodoView
